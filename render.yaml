services:
  - type: web
    name: billiard-hall
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
